
define([
	"app",
], function(app) {
	app.registerDirective("wikiHtml", ["$compile", "$location", function($compile, $location){
		link: function($scope, $element, $attrs) {
			//$scope
		}
	}]);
});
